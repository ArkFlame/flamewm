# creamui-jsx

Runtime support for the `jsx!` and `abi_jsx!` macros from `creamui-macros`.

Applications normally depend on `creamui-macros`; add this crate directly only when implementing a custom component result or JSX integration.

Part of [CreamUI](https://github.com/sammwyy/creamui), licensed under Apache-2.0.
